namespace dExcel.Utilities;

/// <summary>
/// A collection of strings that commonly appear in 'ExcelFunction' descriptions.
/// </summary>
public static class DescriptionUtils
{
    public const string Handle =
        "The 'handle' or name used to refer to the object in memory.\n " +
        "Each object in a workbook must have a a unique handle.";
}
